## エラー修正済み
### エラー1: 管理画面で、status表示を変更しても画面が反応しない。
   - app/admin/threads/[id]/page.tsx
   - クライアントコンポーネントに変更し、useEffectを使用
      - URL の ?status= が変わるたびに再フェッチされ、status表示や一覧が切り替わるようにした
   
### エラー2: エラーメッセージ

```
Error: Route "/api/admin/threads/[id]" used `params.id`. `params` should be awaited before using its properties. Learn more: https://nextjs.org/docs/messages/sync-dynamic-apis
    at GET (app/api/admin/threads/[id]/route.ts:21:28)
  19 | export async function GET(_req: Request, { params }: { params: { id: string } }) {
  20 |     const thread = await prisma.thread.findUnique({
> 21 |         where: { id: params.id },
     |                            ^
  22 |         include: { messages: { orderBy: { createdAt: "asc" }}},
  23 |     });
```

- params を Promise 型で受け取り await してから id を使うようにした
